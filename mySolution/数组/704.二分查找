class Solution {
    public int search(int[] nums, int target) {

        int ans=-1;
        ans=Arrays.binarySearch(nums,target);
        if(ans>=0)
        return ans;
        else
        return -1;

    }
}
