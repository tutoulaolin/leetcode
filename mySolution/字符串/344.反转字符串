class Solution {
    public void reverseString(char[] s) {
        int len=s.length;
        char temp;
        for(int i=0;i<len;i++)
        {
            temp=s[i];
            s[i]=s[len-1];
            s[len-1]=temp;
            len--;
        }
        

    }
}
